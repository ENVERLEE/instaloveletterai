import React from 'react';
import ReactDOM from 'react-dom'; // react-dom 모듈에서 가져옵니다.
import './index.css';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
